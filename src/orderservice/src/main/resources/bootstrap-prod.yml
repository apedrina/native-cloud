spring:
  application:
    name: order
  cloud:
    config:
      failFast: true
      uri: http://config:8888
      username: user
      password: test
      retry:
        initialInterval: 12000
        maxInterval: 12000
        maxAttempts: 30

ribbon:
  eureka:
    enabled: false

